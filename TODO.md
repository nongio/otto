# TODO

- Clarify whether transactions apply atomically across multiple style surfaces only within a single client connection, or if there is a cross-client story.
- Confirm that "scoped per client connection" is intentional and document any implications for multi-client coordination.
- Define transform matrix layout (row/column order) and composition order in the protocol text.
- Specify coordinate units for fixed values (position, size, offsets, radii).
- Align naming for visibility control (consider set_visibility) and polish proper noun capitalization.
